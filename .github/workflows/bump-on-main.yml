name: Bump on main

on:
  push:
    branches: [ "main" ]

jobs:
  bump:
    uses: developer-si/dvgroup/.github/workflows/bump-and-tag.yml@v1
    permissions:
      contents: write
    with:
      node_version: '18'
